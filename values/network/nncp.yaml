nncp:
  enable: true
  nncps:

  - name: 1
    interfaces:
    - name: br1
      type: linux-bridge
      state: up
      ports:
      - ens10
    nodeSelector:
      enable: true
      key: node-role.kubernetes.io/worker
      value: ""

  